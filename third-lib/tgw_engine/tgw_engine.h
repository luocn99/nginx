#ifndef __TGW_ENGINE_H__
#include <stdio.h>
#include <string.h>
/*
static int e_gmp_init(ENGINE *e);
static int e_gmp_finish(ENGINE *e);
static int e_gmp_ctrl(ENGINE *e, int cmd, long i, void *p, void (*f) (void));
*/
void ENGINE_load_tgw(void);

#endif
